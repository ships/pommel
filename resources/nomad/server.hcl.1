# Increase log verbosity
log_level = "DEBUG"

# Setup data dir
data_dir = "/etc/nomad.d"

bind_addr = "192.168.1.41"

# Enable the server
server {
    enabled = true

    bootstrap_expect = 3

      server_join {
        retry_join = ["192.168.1.40:4648"]
      }
}

